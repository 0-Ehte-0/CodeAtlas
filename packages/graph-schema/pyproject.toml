[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codeatlas-graph-schema"
version = "0.1.0"
description = "Neo4j schema conventions and definitions for CodeAtlas"
dependencies = []