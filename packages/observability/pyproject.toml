[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codeatlas-observability"
version = "0.1.0"
description = "Standardized logging utility for CodeAtlas"
dependencies = []