[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codeatlas-queue"
version = "0.1.0"
description = "Redis queue definitions and payloads for CodeAtlas"
dependencies = [
    "pydantic>=2.0.0"
]