[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codeatlas-contracts"
version = "0.1.0"
description = "Shared Pydantic domain models for CodeAtlas"
dependencies = [
    "pydantic>=2.0.0"
]